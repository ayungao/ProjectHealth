<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.health.dao.OrderSettingDao">

    <!--    添加预约设置-->
    <insert id="add" parameterType="com.itheima.health.pojo.OrderSetting">
        insert into t_ordersetting (orderDate, number, reservations)
        values (#{orderDate}, #{number}, #{reservations})
    </insert>

    <!--    更新可预约数-->
    <update id="updateNumber" parameterType="com.itheima.health.pojo.OrderSetting">
        update t_ordersetting
        set number=#{number}
        where orderDate = #{orderDate}
    </update>

    <!--    通过日期查询预约设置信息-->
    <select id="findByOrderDate" parameterType="Date" resultType="com.itheima.health.pojo.OrderSetting">
        select *
        from t_ordersetting
        where orderDate = #{orderDate}
    </select>

    <!--    通过月份查询预约设置信息-->
    <select id="getOrderSettingByMonth" parameterType="map" resultType="java.util.Map">
        select cast(DATE_FORMAT(ORDERDATE, '%d') as SIGNED) date,number,reservations
        from t_ordersetting
        where orderDate between #{startDate}
          and #{endDate}
    </select>
</mapper>